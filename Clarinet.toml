[project]
name = 'waste-collection-payment-pay-only-after-verified-g'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.payment]
path = 'contracts/payment.clar'
clarity_version = 3
epoch = '3.2'

[contracts.registry]
path = 'contracts/registry.clar'
clarity_version = 3
epoch = 'latest'

[contracts.verification]
path = 'contracts/verification.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
